import { SafeHtml } from "@angular/platform-browser";

export interface AboutList {
  icon: SafeHtml;
  title: string;
  description: string;
}

export interface AboutBottom {
  icon: SafeHtml;
  title: string;
}
